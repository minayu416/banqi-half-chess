/** @type {import('next').NextConfig} */
const nextConfig = {
    // 暫時擺上去，作為靜態輸出
    // 可是沒有build 成功

    // reactStrictMode: true,
    // basePath: '/staging',
    // output: 'export',
};

export default nextConfig;
